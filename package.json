{
  "name": "germ-theory",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "expect.js": "^0.3.1",
    "grunt": "^0.4.5",
    "istanbul": "^0.3.2",
    "mocha": "^1.20.1",
    "should": "^4.1.0",
    "supertest": "^0.14.0"
  },
  "dependencies": {
    "bcrypt-nodejs": "0.0.3",
    "body-parser": "^1.4.3",
    "bookshelf": "^0.7.9",
    "express": "^4.4.5",
    "jwt-simple": "^0.2.0",
    "knex": "^0.7.3",
    "morgan": "^1.1.1",
    "passport": "^0.2.0",
    "passport-local": "^1.0.0",
    "pg": "^3.6.2",
    "q": "^1.0.1",
    "request": "^2.47.0"
  },
  "engines": {
    "node": ">= 0.8.0"
  },
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks --require should specs/server/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks --require should specs/server/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --check-leaks --require should specs/server/"
  }
}
